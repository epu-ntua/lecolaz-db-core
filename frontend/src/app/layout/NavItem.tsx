export default function Sidebar() {
  return null;
}
// TODO: Extend module definition with:
// - role-based visibility (allowedRoles: string[])
// - badge support (badgeCount?: number | () => number)
// - dynamic badge styling
// - tooltip when sidebar is collapsed
